import 'package:flutter/material.dart';

class NoRouteFinded extends StatelessWidget {
  const NoRouteFinded({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("No Rout Founded")),
    );
  }
}
